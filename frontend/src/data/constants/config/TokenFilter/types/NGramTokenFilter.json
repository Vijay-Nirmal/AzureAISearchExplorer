{
  "discriminatorValue": "#Microsoft.Azure.Search.NGramTokenFilter",
  "label": "NGramTokenFilter",
  "description": "Generates n-grams of the given size(s). Implemented using Apache Lucene.",
  "fields": [
    {
      "key": "maxGram",
      "label": "Max Gram",
      "type": "number",
      "default": 2,
      "min": 1,
      "tooltip": "The maximum n-gram length. Default is 2."
    },
    {
      "key": "minGram",
      "label": "Min Gram",
      "type": "number",
      "default": 1,
      "min": 1,
      "tooltip": "The minimum n-gram length. Default is 1. Must be less than the value of maxGram."
    }
  ]
}
