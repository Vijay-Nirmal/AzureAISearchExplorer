{
  "discriminatorValue": "#Microsoft.Azure.Search.AsciiFoldingTokenFilter",
  "label": "AsciiFoldingTokenFilter",
  "description": "Converts alphabetic, numeric, and symbolic Unicode characters which are not in the first 127 ASCII characters (the \"Basic Latin\" Unicode block) into their ASCII equivalents, if such equivalents exist. Implemented using Apache Lucene.",
  "fields": [
    {
      "key": "preserveOriginal",
      "label": "Preserve Original",
      "type": "boolean",
      "default": false,
      "tooltip": "A value indicating whether the original token will be kept. Default is false."
    }
  ]
}
