{
  "discriminatorValue": "#Microsoft.Azure.Search.LengthTokenFilter",
  "label": "LengthTokenFilter",
  "description": "Removes words that are too long or too short. Implemented using Apache Lucene.",
  "fields": [
    {
      "key": "max",
      "label": "Max",
      "type": "number",
      "default": 300,
      "min": 0,
      "max": 300,
      "tooltip": "The maximum length in characters. Default and maximum is 300."
    },
    {
      "key": "min",
      "label": "Min",
      "type": "number",
      "default": 0,
      "min": 0,
      "max": 300,
      "tooltip": "The minimum length in characters. Default is 0. Maximum is 300. Must be less than the value of max."
    }
  ]
}
