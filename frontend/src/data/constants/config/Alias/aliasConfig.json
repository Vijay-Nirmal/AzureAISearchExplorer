{
  "entity": { "$ref": "Alias/entities/SearchAlias.json" },
  "commonFields": [
    {
      "key": "name",
      "label": "Name",
      "type": "string",
      "required": true,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9 _-]*[A-Za-z0-9])?$",
      "placeholder": "e.g. products-active",
      "tooltip": "The name of the alias. It must only contain letters, digits, spaces, dashes or underscores, can only start and end with alphanumeric characters, and is limited to 128 characters."
    }
  ],
  "types": [
    { "$ref": "Alias/types/SearchAlias.json" }
  ]
}
