{
  "discriminatorValue": "azureBlob",
  "label": "AzureBlobKnowledgeSource",
  "description": "Knowledge source targeting Azure Blob Storage.",
  "fields": [
    {
      "key": "description",
      "label": "Description",
      "type": "string",
      "tooltip": "Optional user-defined description."
    },
    {
      "key": "encryptionKey",
      "label": "Encryption Key",
      "type": "object",
      "tooltip": "A description of an encryption key that you create in Azure Key Vault.",
      "schema": { "$ref": "Common/EncryptionKey/types/SearchResourceEncryptionKey.json" }
    },
    {
      "key": "azureBlobParameters",
      "label": "Azure Blob Parameters",
      "type": "object",
      "tooltip": "Parameters for Azure Blob Storage knowledge source.",
      "schema": { "$ref": "KnowledgeSource/AzureBlobKnowledgeSource/types/AzureBlobKnowledgeSourceParameters.json" }
    }
  ]
}
