{
  "discriminatorValue": "IndexingParameters",
  "label": "IndexingParameters",
  "description": "Represents parameters for indexer execution.",
  "fields": [
    {
      "key": "batchSize",
      "label": "Batch Size",
      "type": "number",
      "tooltip": "The number of items that are read from the data source and indexed as a single batch in order to improve performance."
    },
    {
      "key": "maxFailedItems",
      "label": "Max Failed Items",
      "type": "number",
      "default": 0,
      "tooltip": "The maximum number of items that can fail indexing for indexer execution to still be considered successful. -1 means no limit. Default is 0."
    },
    {
      "key": "maxFailedItemsPerBatch",
      "label": "Max Failed Items Per Batch",
      "type": "number",
      "default": 0,
      "tooltip": "The maximum number of items in a single batch that can fail indexing for the batch to still be considered successful. -1 means no limit. Default is 0."
    },
    {
      "key": "configuration",
      "label": "Configuration",
      "type": "object",
      "tooltip": "A dictionary of indexer-specific configuration properties.",
      "schema": { "$ref": "Indexer/types/IndexingParametersConfiguration.json" }
    }
  ]
}
