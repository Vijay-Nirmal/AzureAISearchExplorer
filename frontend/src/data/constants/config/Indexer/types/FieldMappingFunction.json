{
  "discriminatorValue": "FieldMappingFunction",
  "label": "FieldMappingFunction",
  "description": "Represents a function that transforms a value from a data source before indexing.",
  "fields": [
    {
      "key": "name",
      "label": "Name",
      "type": "string",
      "tooltip": "The name of the field mapping function."
    },
    {
      "key": "parameters",
      "label": "Parameters",
      "type": "object",
      "tooltip": "A dictionary of parameter name/value pairs to pass to the function. Each value must be of a primitive type."
    }
  ]
}
