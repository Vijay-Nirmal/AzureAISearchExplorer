{
  "discriminatorValue": "#Microsoft.Skills.Util.DocumentIntelligenceLayoutSkill",
  "label": "DocumentIntelligenceLayoutSkill",
  "description": "A skill that extracts content and layout information, via Azure AI Services, from files within the enrichment pipeline.",
  "fields": [
    {
      "key": "chunkingProperties",
      "label": "Chunking Properties",
      "type": "object",
      "tooltip": "Controls the cardinality for chunking the content.",
      "schema": { "$ref": "Skillset/types/DocumentIntelligenceLayoutSkillChunkingProperties.json" }
    },
    {
      "key": "context",
      "label": "Context",
      "type": "string",
      "tooltip": "Represents the level at which operations take place, such as the document root or document content (for example, /document or /document/content). The default is /document."
    },
    {
      "key": "description",
      "label": "Description",
      "type": "string",
      "tooltip": "The description of the skill which describes the inputs, outputs, and usage of the skill."
    },
    {
      "key": "extractionOptions",
      "label": "Extraction Options",
      "type": "enumArray",
      "tooltip": "Controls the cardinality of the content extracted from the document by the skill.",
      "options": { "$ref": "Skillset/Skills/enums/DocumentIntelligenceLayoutSkillExtractionOptions.json" }
    },
    {
      "key": "inputs",
      "label": "Inputs",
      "type": "objectArray",
      "tooltip": "Inputs of the skills could be a column in the source data set, or the output of an upstream skill.",
      "schema": { "$ref": "Skillset/types/InputFieldMappingEntry.json" }
    },
    {
      "key": "markdownHeaderDepth",
      "label": "Markdown Header Depth",
      "type": "enum",
      "tooltip": "The depth of headers in the markdown output. Default is h6.",
      "options": { "$ref": "Skillset/Skills/enums/DocumentIntelligenceLayoutSkillMarkdownHeaderDepth.json" }
    },
    {
      "key": "name",
      "label": "Name",
      "type": "string",
      "tooltip": "The name of the skill which uniquely identifies it within the skillset. A skill with no name defined will be given a default name of its 1-based index in the skills array, prefixed with the character '#'."
    },
    {
      "key": "outputFormat",
      "label": "Output Format",
      "type": "enum",
      "tooltip": "Controls the output format. Default is 'markdown'.",
      "options": { "$ref": "Skillset/Skills/enums/DocumentIntelligenceLayoutSkillOutputFormat.json" }
    },
    {
      "key": "outputMode",
      "label": "Output Mode",
      "type": "enum",
      "tooltip": "Controls the cardinality of the output produced by the skill. Default is 'oneToMany'.",
      "options": { "$ref": "Skillset/Skills/enums/DocumentIntelligenceLayoutSkillOutputMode.json" }
    },
    {
      "key": "outputs",
      "label": "Outputs",
      "type": "objectArray",
      "tooltip": "The output of a skill is either a field in a search index, or a value that can be consumed as an input by another skill.",
      "schema": { "$ref": "Skillset/types/OutputFieldMappingEntry.json" }
    }
  ]
}
