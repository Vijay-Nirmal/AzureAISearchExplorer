{
  "discriminatorValue": "#Microsoft.Skills.Vision.OcrSkill",
  "label": "OcrSkill",
  "description": "A skill that extracts text from image files.",
  "fields": [
    {
      "key": "defaultLanguageCode",
      "label": "Default Language Code",
      "type": "enum",
      "tooltip": "A value indicating which language code to use. Default is en.",
      "options": { "$ref": "Skillset/Skills/enums/OcrSkillLanguage.json" }
    },
    {
      "key": "detectOrientation",
      "label": "Detect Orientation",
      "type": "boolean",
      "tooltip": "A value indicating to turn orientation detection on or not. Default is false."
    },
    {
      "key": "lineEnding",
      "label": "Line Ending",
      "type": "enum",
      "tooltip": "Defines the sequence of characters to use between the lines of text recognized by the OCR skill. The default value is \"space\".",
      "options": { "$ref": "Skillset/Skills/enums/OcrLineEnding.json" }
    }
  ]
}
