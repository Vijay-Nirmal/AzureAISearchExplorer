{
  "discriminatorValue": "#Microsoft.Skills.Text.LanguageDetectionSkill",
  "label": "LanguageDetectionSkill",
  "description": "A skill that detects the language of input text and reports a single language code for every document submitted on the request. The language code is paired with a score indicating the confidence of the analysis.",
  "fields": [
    {
      "key": "defaultCountryHint",
      "label": "Default Country Hint",
      "type": "string",
      "tooltip": "A country code to use as a hint to the language detection model if it cannot disambiguate the language."
    },
    {
      "key": "modelVersion",
      "label": "Model Version",
      "type": "string",
      "tooltip": "The version of the model to use when calling the Text Analytics service. It will default to the latest available when not specified. We recommend you do not specify this value unless absolutely necessary."
    }
  ]
}
