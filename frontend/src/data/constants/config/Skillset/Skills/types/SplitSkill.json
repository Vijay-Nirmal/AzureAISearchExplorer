{
  "discriminatorValue": "#Microsoft.Skills.Text.SplitSkill",
  "label": "SplitSkill",
  "description": "A skill to split a string into chunks of text.",
  "fields": [
    {
      "key": "azureOpenAITokenizerParameters",
      "label": "Azure Open AITokenizer Parameters",
      "type": "object",
      "tooltip": "Only applies if the unit is set to azureOpenAITokens. If specified, the splitSkill will use these parameters when performing the tokenization. The parameters are a valid 'encoderModelName' and an optional 'allowedSpecialTokens' property.",
      "schema": { "$ref": "Skillset/types/AzureOpenAITokenizerParameters.json" }
    },
    {
      "key": "defaultLanguageCode",
      "label": "Default Language Code",
      "type": "enum",
      "tooltip": "A value indicating which language code to use. Default is en.",
      "options": { "$ref": "Skillset/Skills/enums/SplitSkillLanguage.json" }
    },
    {
      "key": "maximumPageLength",
      "label": "Maximum Page Length",
      "type": "number",
      "tooltip": "The desired maximum page length. Default is 10000."
    },
    {
      "key": "maximumPagesToTake",
      "label": "Maximum Pages To Take",
      "type": "number",
      "tooltip": "Only applicable when textSplitMode is set to 'pages'. If specified, the SplitSkill will discontinue splitting after processing the first 'maximumPagesToTake' pages, in order to improve performance when only a few initial pages are needed from each document."
    },
    {
      "key": "pageOverlapLength",
      "label": "Page Overlap Length",
      "type": "number",
      "tooltip": "Only applicable when textSplitMode is set to 'pages'. If specified, n+1th chunk will start with this number of characters/tokens from the end of the nth chunk."
    },
    {
      "key": "textSplitMode",
      "label": "Text Split Mode",
      "type": "enum",
      "tooltip": "A value indicating which split mode to perform.",
      "options": { "$ref": "Skillset/Skills/enums/TextSplitMode.json" }
    },
    {
      "key": "unit",
      "label": "Unit",
      "type": "enum",
      "tooltip": "Only applies if textSplitMode is set to pages. There are two possible values. The choice of the values will decide the length (maximumPageLength and pageOverlapLength) measurement. The default is 'characters', which means the length will be measured by character.",
      "options": { "$ref": "Skillset/Skills/enums/SplitSkillUnit.json" }
    }
  ]
}
