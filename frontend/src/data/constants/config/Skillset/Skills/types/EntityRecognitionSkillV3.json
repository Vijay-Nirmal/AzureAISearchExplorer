{
  "discriminatorValue": "#Microsoft.Skills.Text.V3.EntityRecognitionSkill",
  "label": "EntityRecognitionSkillV3",
  "description": "Using the Text Analytics API, extracts entities of different types from text.",
  "fields": [
    {
      "key": "categories",
      "label": "Categories",
      "type": "enumArray",
      "tooltip": "A list of entity categories that should be extracted.",
      "options": { "$ref": "Skillset/Skills/enums/EntityCategory.json" }
    },
    {
      "key": "defaultLanguageCode",
      "label": "Default Language Code",
      "type": "string",
      "tooltip": "A value indicating which language code to use. Default is en."
    },
    {
      "key": "minimumPrecision",
      "label": "Minimum Precision",
      "type": "number",
      "tooltip": "A value between 0 and 1 that be used to only include entities whose confidence score is greater than the value specified. If not set (default), or if explicitly set to null, all entities will be included."
    },
    {
      "key": "modelVersion",
      "label": "Model Version",
      "type": "string",
      "tooltip": "The version of the model to use when calling the Text Analytics API. It will default to the latest available when not specified. We recommend you do not specify this value unless absolutely necessary."
    }
  ]
}
