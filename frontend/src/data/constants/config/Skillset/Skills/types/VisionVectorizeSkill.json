{
  "discriminatorValue": "#Microsoft.Skills.Vision.VectorizeSkill",
  "label": "VisionVectorizeSkill",
  "description": "Allows you to generate a vector embedding for a given image or text input using the Azure AI Services Vision Vectorize API.",
  "fields": [
    {
      "key": "modelVersion",
      "label": "Model Version",
      "type": "string",
      "tooltip": "The version of the model to use when calling the AI Services Vision service. It will default to the latest available when not specified."
    }
  ]
}
