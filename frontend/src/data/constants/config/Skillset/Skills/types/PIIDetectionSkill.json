{
  "discriminatorValue": "#Microsoft.Skills.Text.PIIDetectionSkill",
  "label": "PIIDetectionSkill",
  "description": "Using the Text Analytics API, extracts personal information from an input text and gives you the option of masking it.",
  "fields": [
    {
      "key": "defaultLanguageCode",
      "label": "Default Language Code",
      "type": "string",
      "tooltip": "A value indicating which language code to use. Default is en."
    },
    {
      "key": "domain",
      "label": "Domain",
      "type": "string",
      "tooltip": "If specified, will set the PII domain to include only a subset of the entity categories. Possible values include: 'phi', 'none'. Default is 'none'."
    },
    {
      "key": "maskingCharacter",
      "label": "Masking Character",
      "type": "string",
      "tooltip": "The character used to mask the text if the maskingMode parameter is set to replace. Default is '*'."
    },
    {
      "key": "maskingMode",
      "label": "Masking Mode",
      "type": "enum",
      "tooltip": "A parameter that provides various ways to mask the personal information detected in the input text. Default is 'none'.",
      "options": { "$ref": "Skillset/Skills/enums/PIIDetectionSkillMaskingMode.json" }
    },
    {
      "key": "minimumPrecision",
      "label": "Minimum Precision",
      "type": "number",
      "tooltip": "A value between 0 and 1 that be used to only include entities whose confidence score is greater than the value specified. If not set (default), or if explicitly set to null, all entities will be included."
    },
    {
      "key": "modelVersion",
      "label": "Model Version",
      "type": "string",
      "tooltip": "The version of the model to use when calling the Text Analytics service. It will default to the latest available when not specified. We recommend you do not specify this value unless absolutely necessary."
    },
    {
      "key": "piiCategories",
      "label": "PII Categories",
      "type": "stringArray",
      "tooltip": "A list of PII entity categories that should be extracted and masked."
    }
  ]
}
