{
  "discriminatorValue": "#Microsoft.Skills.Util.DocumentExtractionSkill",
  "label": "DocumentExtractionSkill",
  "description": "A skill that extracts content from a file within the enrichment pipeline.",
  "fields": [
    {
      "key": "configuration",
      "label": "Configuration",
      "type": "string",
      "tooltip": "A dictionary of configurations for the skill."
    },
    {
      "key": "dataToExtract",
      "label": "Data To Extract",
      "type": "string",
      "tooltip": "The type of data to be extracted for the skill. Will be set to 'contentAndMetadata' if not defined."
    },
    {
      "key": "parsingMode",
      "label": "Parsing Mode",
      "type": "string",
      "tooltip": "The parsingMode for the skill. Will be set to 'default' if not defined."
    }
  ]
}
