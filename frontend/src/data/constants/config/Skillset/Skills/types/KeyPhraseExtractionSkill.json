{
  "discriminatorValue": "#Microsoft.Skills.Text.KeyPhraseExtractionSkill",
  "label": "KeyPhraseExtractionSkill",
  "description": "A skill that uses text analytics for key phrase extraction.",
  "fields": [
    {
      "key": "defaultLanguageCode",
      "label": "Default Language Code",
      "type": "enum",
      "tooltip": "A value indicating which language code to use. Default is en.",
      "options": { "$ref": "Skillset/Skills/enums/KeyPhraseExtractionSkillLanguage.json" }
    },
    {
      "key": "maxKeyPhraseCount",
      "label": "Max Key Phrase Count",
      "type": "number",
      "tooltip": "A number indicating how many key phrases to return. If absent, all identified key phrases will be returned."
    },
    {
      "key": "modelVersion",
      "label": "Model Version",
      "type": "string",
      "tooltip": "The version of the model to use when calling the Text Analytics service. It will default to the latest available when not specified. We recommend you do not specify this value unless absolutely necessary."
    }
  ]
}
