{
  "discriminatorValue": "#Microsoft.Skills.Text.V3.SentimentSkill",
  "label": "SentimentSkillV3",
  "description": "Using the Text Analytics API, evaluates unstructured text and for each record, provides sentiment labels (such as \"negative\", \"neutral\" and \"positive\") based on the highest confidence score found by the service at a sentence and document-level.",
  "fields": [
    {
      "key": "defaultLanguageCode",
      "label": "Default Language Code",
      "type": "string",
      "tooltip": "A value indicating which language code to use. Default is en."
    },
    {
      "key": "includeOpinionMining",
      "label": "Include Opinion Mining",
      "type": "boolean",
      "tooltip": "If set to true, the skill output will include information from Text Analytics for opinion mining, namely targets (nouns or verbs) and their associated assessment (adjective) in the text. Default is false."
    },
    {
      "key": "modelVersion",
      "label": "Model Version",
      "type": "string",
      "tooltip": "The version of the model to use when calling the Text Analytics service. It will default to the latest available when not specified. We recommend you do not specify this value unless absolutely necessary."
    }
  ]
}
