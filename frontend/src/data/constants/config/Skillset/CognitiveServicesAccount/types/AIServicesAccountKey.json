{
  "discriminatorValue": "#Microsoft.Azure.Search.AIServicesByKey",
  "label": "AI Services (Key + Subdomain)",
  "description": "Attach an Azure AI Services resource using an API key and subdomain URL.",
  "fields": [
    {
      "key": "key",
      "label": "Key",
      "type": "string",
      "tooltip": "The key used to provision the Azure AI service resource attached to a skillset.",
      "placeholder": "<api key>"
    },
    {
      "key": "subdomainUrl",
      "label": "Subdomain URL",
      "type": "string",
      "tooltip": "The subdomain URL for the corresponding AI Service.",
      "placeholder": "https://<resource>.cognitiveservices.azure.com"
    }
  ]
}
