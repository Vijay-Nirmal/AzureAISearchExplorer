[
  {
    "value": "postFilter",
    "description": "The filter is applied after the candidate set of vector results is returned. Depending on filter selectivity, this can result in fewer results than requested by k."
  },
  { "value": "preFilter", "description": "The filter is applied before the vector search is performed." }
]
