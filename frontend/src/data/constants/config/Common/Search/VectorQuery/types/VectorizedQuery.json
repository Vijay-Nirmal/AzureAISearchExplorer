{
  "discriminatorValue": "vector",
  "label": "VectorizedQuery",
  "description": "Vector search with a provided raw vector array.",
  "fields": [
    {
      "key": "fields",
      "label": "Fields",
      "type": "string",
      "required": true,
      "placeholder": "vectorField",
      "tooltip": "Vector fields of type Collection(Edm.Single) to include in the vector search."
    },
    {
      "key": "k",
      "label": "K",
      "type": "number",
      "required": true,
      "min": 1,
      "tooltip": "Number of nearest neighbors to return as top hits."
    },
    {
      "key": "vector",
      "label": "Vector",
      "type": "object",
      "tooltip": "Raw vector array (number[]). Use Edit JSON to paste an array of floats."
    },
    {
      "key": "filterOverride",
      "label": "Filter Override",
      "type": "string",
      "tooltip": "OData filter expression to apply to this specific vector query."
    },
    {
      "key": "perDocumentVectorLimit",
      "label": "Per Document Vector Limit",
      "type": "number",
      "min": 0,
      "tooltip": "How many vectors can be matched from each document (0 = unlimited)."
    },
    {
      "key": "threshold",
      "label": "Threshold",
      "type": "object",
      "tooltip": "Threshold used for vector queries.",
      "schema": { "$ref": "Common/Search/VectorThreshold/vectorThresholdConfig.json" }
    },
    {
      "key": "weight",
      "label": "Weight",
      "type": "number",
      "min": 0,
      "default": 1,
      "tooltip": "Relative weight of this vector query compared to other vector/text queries."
    },
    {
      "key": "exhaustive",
      "label": "Exhaustive",
      "type": "boolean",
      "default": false,
      "tooltip": "When true, triggers an exhaustive k-nearest neighbor search across all vectors within the vector index."
    },
    {
      "key": "oversampling",
      "label": "Oversampling",
      "type": "number",
      "min": 1,
      "tooltip": "Oversampling factor. Minimum value is 1."
    }
  ]
}
